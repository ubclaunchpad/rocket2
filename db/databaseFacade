class DatabaseFacade:
    db;
def __init__():
    db = Database();

def store_user(user):
    db.store(user)

def retrieve_user(id):
    return db.retrieve(id)

def query_user(parameter):
    return db.query(parameter)
