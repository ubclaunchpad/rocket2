language: python
python:
    - "3.7-dev"
install:
    - pip install pipenv
    - pipenv install --dev
    - gem install mdl
script:
    - pycodestyle .
    - mdl .
    - cd tests/ && pytest --cov=./
    - codecov
