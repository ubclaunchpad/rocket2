testing = true
creds_path = "credentials/"

[slack]
bot_channel = "#rocket2"

[aws]
users_table = "users"
teams_table = "teams"
projects_table = "projects"
region = "us-west-2"

[github]
app_id = 31754
organization = "ubclaunchpad-cytesting"
webhook_url = "/webhook"

[auth]
signing_key_path = "credentials/github_signing_key.pem"
